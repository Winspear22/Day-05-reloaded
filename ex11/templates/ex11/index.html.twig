{% extends 'base.html.twig' %}

{% block title %}Exercise 11 - Test Tables{% endblock %}

{% block body %}

{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label }}">{{ message }}</div>
    {% endfor %}
{% endfor %}

<div style="max-width: 600px; margin: 50px auto; padding: 20px;">
    <h1>Exercise 11 - Test</h1>

    <!-- Créer les tables -->
    <form method="POST" action="{{ path('ex11_create_all_tables') }}" style="margin: 20px 0;">
        <button type="submit" style="padding: 12px 30px; background: #28a745; color: white; border: none; border-radius: 4px; cursor: pointer; font-size: 16px;">
            ✓ Créer les Tables
        </button>
    </form>

    <!-- Charger les données de démo -->
    <form method="POST" action="{{ path('ex11_load_demo_data') }}" style="margin: 20px 0;">
        <button type="submit" style="padding: 12px 30px; background: #17a2b8; color: white; border: none; border-radius: 4px; cursor: pointer; font-size: 16px;">
            ⚡ Charger Données de Démo
        </button>
    </form>

    <!-- Supprimer toutes les données -->
    <form method="POST" action="{{ path('ex11_delete_all_tables') }}" style="margin: 20px 0;">
        <button type="submit" style="padding: 12px 30px; background: #dc3545; color: white; border: none; border-radius: 4px; cursor: pointer; font-size: 16px;" onclick="return confirm('Êtes-vous sûr ?')">
            🗑️ Supprimer Toutes les Données
        </button>
    </form>
</div>

{% endblock %}
